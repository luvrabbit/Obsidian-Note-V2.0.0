# 求广义逆的方法

## 行列分解
$A=BC$,$A^{\dagger}=C^{T}(CC^{T})^{-1}(B^{T}B)^{-1}B^{T}$
通过维数进行记忆，$A^{\dagger}_{n×m},B_{m×r},C_{r×n}$，
## SVD分解

# 广义逆的性质
1. $AA^{\dagger} = (AA^{\dagger})^{T}$
2. $A^{\dagger}A = (A^{\dagger}A)^{T}$
3. $AA^{\dagger}A = A^{\dagger}$
4. $A^{\dagger}AA^{\dagger} = A$
5. $A^{\dagger} = (A^{T}A)^{\dagger}A^{T} = A^{T}()$
