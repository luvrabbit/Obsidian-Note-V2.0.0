# 插值的基本想法
找到一组多项式$\mathcal{P}(x)$的基函数，通过这组基函数的组合来拟合原函数$f(x)$
即：
$$
f(x) ≈ p(x)=a_{0}\phi_{0}(x)+a_{1}\phi_{1}(x)+\dots+a_{n}\phi_{n}(x)
$$

# 拉格朗日插值
确定基函数:
$$
l_{k}(x_{i})=
\begin{cases}
1 & \text{if} & i=k  \\
0 & \text{if} & i\neq k
\end{cases}
$$